# Pointers

This section covers memory, memory addresses, address and dereference operators, pointer variables, and pointer arithmetic in C++.
It also covers null pointers, pointer to pointer, pointers with arrays/functions, function pointers, and `nullptr`.
