# Functions

This section covers function declaration, definition, calling, parameters, arguments, and return values in C++.
It also covers `void` functions, default arguments, function overloading, inline functions, recursion, and lambda functions.
