# Conditional Statements

This section covers decision-making in C++ using `if`, `if-else`, `else-if`, and nested `if`.
It also covers `switch`, `case`, `default`, `break`, and the Conditional/Ternary Operator.
